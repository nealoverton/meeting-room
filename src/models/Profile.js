class Profile {
    constructor(uid, email, name, colour, avatar) {
        this.uid = uid;
        this.email = email;
        this.name = name;
        this.colour = colour;
        this.avatar = avatar;
    }
}

export default Profile;